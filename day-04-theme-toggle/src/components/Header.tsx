import ThemeToggle from "./ThemeToggle";

const Header = () => {
    return (
        <header>
            <h1>Theme Toggle App</h1>
            <ThemeToggle />
        </header>
    );
};

export default Header;
